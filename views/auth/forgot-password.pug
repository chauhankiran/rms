extends ../layout

block content
    .row 
        .col-6 
            h1= title

            form(action="/auth/forgot-password?_method=PUT", method="post") 
                .form-group 
                    label(for="email") Email
                    input(type="text", name="email", placeholder="kai@doe.com", autofocus=true)#email
        
                .form-group 
                    button(type="submit") Reset password

                .form-group
                    p 
                        a(href="/auth/login") Go back to login