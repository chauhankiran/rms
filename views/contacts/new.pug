extends ../layout

block content
  .row 
    .col-6 
      h1= title

      form(action="/contacts", method="post") 
        input(type="hidden", name="companyId" value=companyId)

        include ./form
        
        .form-group 
          .row.align-items-center 
            .col-6 
              button(type="submit") Create #{singular(downcase(labels.module.contact))}
            .col-6.text-end
              a(href="/contacts").btn.is-muted Cancel
        