extends ../../../layout

block content
  .row 
    .col-6 
      h1= title

      include ../../../partials/flash

      form(action=`/admin/labels/contacts/${contactLabel.id}?_method=PUT`, method="post") 
        .form-group 
          label(for="displayName") Display name
          input(type="text", name="displayName", id="displayName", value=contactLabel.displayName, placeholder="Visitor", autofocus=true)

        .form-group 
          .row.align-items-center 
            .col-6 
              button(type="submit") Update contact label
            .col-6.text-end
              a(href=`/admin/labels/contacts/${contactLabel.id}`).btn.is-muted Cancel
        