extends ../../layout

block content
  .row 
    .col-6 
      h1= title

      include ../../partials/flash

      form(action="/admin/company-sources", method="post") 
        .form-group 
          label(for="name") Name
          input(type="text", name="name", placeholder="Social Media", autofocus=true)#name
        
        .form-group 
          .row.align-items-center 
            .col-6 
              button(type="submit") Create company source
            .col-6.text-end
              a(href="/admin/company-sources").btn.is-muted Cancel
        