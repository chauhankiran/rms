extends ../../layout

block content
    .row 
        .col-6 
            h1= title

            include ../../partials/flash

            form(action="/admin/industries", method="post") 
                include ./form
        
                .form-group 
                    .row.align-items-center 
                        .col-6 
                            button(type="submit") Create industry
                        .col-6.text-end
                            a(href="/admin/industries").btn.is-muted Cancel
