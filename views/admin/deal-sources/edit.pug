extends ../../layout

block content
  .row 
    .col-6 
      h1= title

      include ../../partials/flash

      form(action=`/admin/deal-sources/${dealSource.id}?_method=PUT`, method="post") 
        include ./form

        .form-group 
          .row.align-items-center 
            .col-6 
              button(type="submit") Update deal source
            .col-6.text-end
              a(href=`/admin/deal-sources/${dealSource.id}`).btn.is-muted Cancel
        