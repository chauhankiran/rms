extends ../../layout

block content
  .row 
    .col-6 
      h1= title

      include ../../partials/flash

      form(action=`/admin/ticket-types/${ticketType.id}?_method=PUT`, method="post") 
        include ./form

        .form-group 
          .row.align-items-center 
            .col-6 
              button(type="submit") Update ticket type
            .col-6.text-end
              a(href=`/admin/ticket-types/${ticketType.id}`).btn.is-muted Cancel
        